fastapi==0.104.1
uvicorn[standard]==0.24.0
pandas==2.1.3
numpy==1.26.2
python-multipart==0.0.6
httpx==0.25.2

# For live TGAT inference in /api/o3/predict (falls back to simulated scores
# if these aren't installed or data/processed + artifacts/models are absent):
# pip install -r ../requirements.txt
